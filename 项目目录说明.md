# AI Hotspot 项目目录说明

> 更新时间：2026-07-22。根目录只保留运行入口、正式原型、工程目录和必要配置。

## 1. 目录职责

| 路径 | 职责 | 是否现行依据 |
|---|---|---|
| `apps/web/` | Next.js 用户端与管理端 | 是 |
| `services/core/` | Spring Boot 核心业务、认证、权限与治理 API | 是 |
| `services/ai/` | FastAPI AI API、Connector、Pipeline 与统一 Worker | 是 |
| `packages/contracts/` | OpenAPI 与跨服务事件契约 | 是 |
| `infra/` | Nginx 和部署配置 | 是 |
| `scripts/` | Docker Compose 辅助、测试、备份和恢复 | 是 |
| `AI Hotspot 项目方案/` | 中文方案；以该目录的 `README.md` 为唯一入口 | 是 |
| `artifacts/` | 本地验收产物，默认不提交 | 否 |
| `archive/` | 已停用的早期方案与原型，只供追溯 | 否 |
| `AI_HOTSPOT_PROTOTYPE.html` | 当前视觉与交互基线 | 是 |
| `compose.yaml` | 唯一标准本地运行入口 | 是 |

## 2. 根目录保留文件

- `README.md`：开发与运行入口；
- `项目目录说明.md`：目录职责与清理规则；
- `.env.example`：无密钥模板；`.env` 仅限本机且禁止提交；
- `package.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml`：前端工作区；
- `.gitignore`、`.dockerignore`、`.gitattributes`、`.node-version`：工程配置；
- `agent-browser.json`：无头浏览器验证配置，不是常驻服务。

根目录不存在需要再拆分的新业务应用。当前“3 个代码应用 + 10 个 Compose 服务”已经是 MVP 的最小清晰边界，不为单个队列或单种 AI 能力额外复制服务。

## 3. 生成目录与清理规则

以下内容可重新生成且不得提交 Git：

- `node_modules/`、`apps/web/node_modules/`；
- `apps/web/.next/`；
- `services/core/target/`；
- `services/ai/.venv/`；
- `.pytest_cache/`、`.ruff_cache/`、`__pycache__/`；
- `artifacts/` 下的临时截图、备份和测试输出。

依赖目录可在本机保留以减少重复安装。清理前先确认不在 Git 跟踪列表中，不得删除用户未提交的业务文件。

## 4. 文档去重规则

1. 方案阅读从 `AI Hotspot 项目方案/README.md` 开始。
2. 当前进度只更新《项目进度与剩余任务》。
3. 当前架构只更新《系统总体架构》。
4. 待办只更新《后续任务清单》。
5. 阶段拆分、评审和验收记录均为历史证据，不再承载“当前状态”。
6. `archive/` 不得覆盖现行方案、代码或正式原型。
