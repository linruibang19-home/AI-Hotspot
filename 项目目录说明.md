# AI Hotspot 项目目录说明

> 当前产品名：AI Hotspot。根目录只保留运行入口、现行正式原型、工程目录和必要配置。

## 目录职责

| 路径 | 职责 |
|---|---|
| `apps/web/` | Next.js 正式 Web 前端 |
| `services/core/` | Spring Boot 核心业务、认证、权限、内容与管理 API |
| `services/ai/` | Python AI API、采集与异步处理 Worker |
| `packages/contracts/` | OpenAPI 与跨服务接口契约 |
| `infra/` | Nginx 和部署基础设施配置 |
| `scripts/` | 启停、重置、Smoke Test 与运维脚本 |
| `AI Hotspot 项目方案/` | 中文产品、架构、数据、测试和交付方案 |
| `artifacts/` | 本地验收截图和测试证据；默认不提交 Git |
| `archive/` | 已停用但需要保留追溯的早期方案与原型 |
| `AI_HOTSPOT_PROTOTYPE.html` | 当前正式交互原型与前端视觉验收基线 |
| `compose.yaml` | 唯一标准本地运行入口 |

## 根目录保留文件

- `README.md`：开发和运行入口；
- `.env.example`：环境变量模板；`.env` 仅供本机使用且不提交；
- `package.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml`：前端工作区；
- `.gitignore`、`.dockerignore`、`.gitattributes`、`.node-version`：工程配置。

## 生成目录与清理规则

以下目录不得提交 Git，可随时重新生成：

- `node_modules/`、`apps/web/node_modules/`；
- `apps/web/.next/`；
- `services/core/target/`；
- `services/ai/.venv/`；
- `.pytest_cache/`、`.ruff_cache/`、`__pycache__/`。

依赖目录虽然体积较大，但保留可避免每次启动重复安装。构建缓存和测试缓存可在阶段验收后清理。

## 归档规则

- 仍被代码、文档或验收流程引用的文件不得移动；
- 已被 AI Hotspot 正式方案替代的 AI Pulse 文档统一放入 `archive/早期 AI Pulse 方案/`；
- 归档不是现行需求来源，不得覆盖 `AI_HOTSPOT_PROTOTYPE.html` 和 `AI Hotspot 项目方案/` 中的已确认决策。
