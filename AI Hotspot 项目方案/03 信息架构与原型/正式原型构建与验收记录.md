# AI Hotspot 正式原型构建与验收记录

> 文档状态：已验收；等待随本次提交合并后创建 `prototype-v1`。

## 1. 原型关系

- 历史参考：根目录 `AI_PULSE_PROTOTYPE.html`，保持原样。
- 正式候选：根目录 `AI_HOTSPOT_PROTOTYPE.html`，由历史参考派生。
- 视觉证据：`01 参考产品与现有材料/AI HOT 界面截图/`。
- 本轮没有编写真实 API、数据库或后端业务，所有数据和交互均为静态 Mock。

## 2. 本轮变更

1. 品牌统一为 AI Hotspot，保留 AI HOT 风格轨道标识。
2. 删除 X/微信公众号内容、筛选、Connector、健康度和正文提示。
3. 主要信源改为官网、RSS、Sitemap、GitHub、Hugging Face、arXiv、OpenReview、Hacker News 和公开媒体社区。
4. “全部动态”文案明确为已准入、PUBLISHED、PUBLIC 的公开池。
5. 内容卡与详情增加官方/一手、UNCONFIRMED 标记；未证实样例不进入精选。
6. 当前热点可进入事件详情；事件展示独立信源、时间线和跨事件关联。
7. 新增订阅与邮件页，展示每日/每周 09:00、Asia/Shanghai、投递、偏好与退订。
8. 模型管理同步 DeepSeek、bge-m3、bge-reranker-v2-m3 和 Mock Provider。
9. 信源新增表单把 display_policy 与 index_policy 分开。

## 3. 继承与偏差台账

| 参考证据 | 正式候选 | 结论 |
|---|---|---|
| 左侧导航与冷灰主区 | 保留固定侧栏、同类配色和卡片 | 继承 |
| 精选热点+时间线 | 保留并让热点可进入事件 | 必要功能扩展 |
| 全部动态日期流 | 保留；公开池规则更严格 | 方案同步 |
| 日报归档/正文结构 | 保留日报入口与报告卡 | 继承；深度正文留正式前端 |
| 主题卡片矩阵 | 保留现有主题网格 | 继承 |
| 收藏列表 | 保留并使用新本地存储 Key | 继承 |
| Agent 接入展示 | 项目方案采用登录后 Agent 工作台 | 有意差异，遵循本项目定位 |
| AI HOT 窄侧栏 | 为完整 AI Hotspot 品牌调整为 188px | 有意差异，不改变整体布局 |

## 4. 已执行静态检查

- 静态 HTTP 服务返回 200。
- JavaScript 经 Node `new Function` 语法编译通过。
- `git diff --check` 通过。
- 正式候选中扫描不到 AI PULSE、公众号、微信、WeChat 或相关 Connector 文案。
- 原始参考文件未被修改。

## 5. 浏览器验收结果

Codex Browser 运行时仍因“failed to write kernel assets: 系统找不到指定的路径 (os error 3)”无法初始化。产品负责人已批准继续执行，因此使用安装在系统临时目录的 Playwright 1.61.1 + Chromium 完成本地回退验证；没有修改项目依赖。

已通过 27 项检查：

- 页面标题、首屏非空和错误覆盖层；
- 精选、全部、日报、主题、收藏、订阅、知识问答、Agent 和四个管理端路由；
- 热点 → 事件 → 订阅完整交互；
- 订阅操作 Toast；
- RAG 演示答案与证据；
- 新增信源弹窗与试抓取；
- 390×844 移动视口、无横向溢出、导航图标；
- 控制台无 error/warn。

截图保存在系统临时目录 `ai-hotspot-m0-qa`，不提交仓库。

## 6. 发现与修复

1. 路由切换后信源弹窗残留：在 `routeTo` 中统一关闭弹窗。
2. 移动端侧栏图标被文字隐藏规则误伤：只隐藏非 `.nav-icon` 的文字节点。

修复后完整回归通过。

## 7. 冻结结论

方案、正式原型、静态检查、桌面/移动视觉和核心交互已通过。合并 `main` 后创建 annotated tag `prototype-v1`，M0 完成并进入已授权的 M1。
