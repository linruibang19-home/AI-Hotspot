# AI Hotspot 新对话交接提示词

> 更新时间：2026-07-22。将下面整个代码块复制到新的 Codex 对话中即可继续。

```text
你现在接手本地项目 AI Hotspot，请直接在现有工程上继续，不要重新搭建项目、不要重做 M0～M10，也不要把历史阶段文档当作当前状态。

工作区绝对路径：
D:\Dev\Autumn recruitment project\AI Hotspot

当前 Git 分支：
codex/ui-fidelity-prototype

开始工作前必须依次完整阅读：
1. AI Hotspot 项目方案/README.md
2. AI Hotspot 项目方案/00 项目总览/项目进度与剩余任务.md
3. AI Hotspot 项目方案/00 项目总览/已确认决策.md
4. AI Hotspot 项目方案/04 系统架构设计/系统总体架构.md
5. AI Hotspot 项目方案/06 数据库与数据治理/数据库表全景与字段中文说明.md
6. AI Hotspot 项目方案/08 AI 能力设计/搜索RAG与Agent详细设计.md
7. AI Hotspot 项目方案/10 测试运维与质量保障/本地中间件与数据库连接指南.md
8. AI Hotspot 项目方案/11 开发与交付计划/后续任务清单.md

当前事实：
- M0～M10 已完成本地产品闭环，尚未完成互联网公开 Beta 的生产环境放行。
- 架构是 3 个代码应用：Next.js Web、Spring Boot Core API、Python AI API/Worker。
- Docker Compose 是唯一标准运行方式；当前共 10 个服务：web、core-api、ai-api、worker、postgres、redis、rabbitmq、minio、mailpit、nginx。
- 单 PostgreSQL 16 + pgvector 实例，8 个业务 Schema、55 张业务表；Flyway 已到 V028。
- 当前真实 Provider：DeepSeek OpenAI-compatible Generation、SiliconFlow BAAI/bge-m3 Embedding、SiliconFlow BAAI/bge-reranker-v2-m3 Rerank；配置与密钥只读本机 .env，绝不能打印、提交或写入文档。
- 122 个 Endpoint，79 个 ACTIVE 且 79 个 HEALTHY；X 与微信公众号不采集，X 只保留禁用扩展契约。
- 2026-07-22 验收快照：内容 2,786、真实公开 1,421、索引文档 1,421、Chunk 3,114、向量 3,114、RAG 独立信源 69、Mock 公开 0、未来时间公开 0、PENDING 死信 0。
- 最新 RAG 基线：50/50 约束通过，Recall@20=1.0、nDCG@10=0.974、引用支持率=1.0、ACL 泄漏=0；M10 readiness=PASS。
- V024 修复了保留任务误用不存在的 iam.email_verification_code 表；真实表是 iam.email_verification_challenge。
- Connector 已修复：Feed 正常但关键词 0 匹配时按“成功且 0 新增”处理，不再制造失败和死信。
- RAG 已修复：只把答案真实引用的证据写入 research.citation；未引用候选上下文不进入引用支持率分母。
- 普通用户开放邮箱验证码注册登录；游客可浏览公开内容；收藏、订阅、RAG、Agent 需登录；管理端仅 ADMIN/OPERATOR。

下一步目标：
按《后续任务清单》进入“信源质量与 RAG 专项”，优先完成：
1. RAG-02：给查询理解、FTS、Embedding、Rerank、生成、引用修复分段计时，把本地 P95 优化到不高于 12 秒且不降低引用质量。
2. SRC-02/SRC-03：继续补来源发布时间并跟踪 Connector 健康语义。
3. 然后推进 RAG-03/RAG-04：冲突证据表达与管理端可观测性。

执行规则：
- 先检查 git status、docker compose ps、数据库和最新日志；保留用户已有修改。
- 只使用真实数据库和真实接口判断完成，禁止用静态 Mock 页面或虚假数据顶替。
- 数据库只能新增 Flyway 迁移，绝不修改已执行的 V001～V028；字段变化同步中文数据库文档。
- 架构保持清晰最小化：不要引入 Kafka、Elasticsearch、Kubernetes 或额外微服务；目前不要引入 LangGraph，除非有真实复杂状态图证据并先写 ADR。
- 常规测试不要启动可见浏览器。需要 UI 回归时使用 agent-browser.json 的无头模式并关闭 Session，避免电脑弹出闪烁窗口。
- 每个小任务必须自行测试；阶段完成时更新“项目进度与剩余任务”“后续任务清单”，汇报完成项、测试证据、已知限制和剩余任务。
- 做好 Git：检查 diff，禁止提交 .env、密钥、备份和临时产物；在当前 codex/ 分支形成语义明确的本地提交。未经用户授权不得 push。

标准验证命令：
pnpm check
services\core\mvnw.cmd -B test -f services\core\pom.xml
services\ai\.venv\Scripts\python.exe -m pytest services\ai\tests -q
docker compose --env-file .env.example config --quiet
scripts\verify-m7-m10.ps1 -RequireBetaReady

完成任何阶段后，必须报告：当前项目进展、运行数据、测试结果、Git 提交和剩余任务。
```
