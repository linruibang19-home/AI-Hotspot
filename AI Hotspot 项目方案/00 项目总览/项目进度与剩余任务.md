# AI Hotspot 项目进度与剩余任务

> 更新时间：2026-07-20；口径以 Flyway、运行中的 Docker Compose、自动化测试和浏览器回归为准。

## 1. 总体结论

M0～M10 已完成本地交付。DeepSeek Generation、SiliconFlow bge-m3 Embedding 与 bge-reranker-v2-m3 Rerank 已启用，历史内容已由真实 Provider 重处理，Mock 内容退出公开产品和 RAG，主题页使用真实数据时间线。2026-07-20 又完成信源时间、公开流多样性和 RAG v2 加固；必须以本轮重新执行的测试和门禁结果为最终口径。这代表本地产品闭环，不等同于互联网公开 Beta；生产 SMTP、Linux/HTTPS、异机恢复和内容合规仍需上线环境验收。

## 2. 各阶段状态

| 阶段 | 当前状态 | 已落地能力 | 仍需外部条件/精修 |
|---|---|---|---|
| M0～M6 | 已通过 | 方案、工程、认证、82 个正式信源目录、采集、内容治理、公开资讯、主题、报告 | 继续做 UI 和信源质量迭代 |
| M7 RAG | 已实现并完成 v2 加固 | V011/V020～V023、数据集/ACL/正文 Chunk、FTS+bge-m3、RRF、Rerank、权限前置、时间/官方过滤、来源与事件多样化、句级引用修复/降级、10 条中文黄金集、配置/评测/监控 | 持续扩充黄金集，进行长问题、冲突事实与线上成本压测 |
| M8 订阅邮件 | 已实现并本地验证 | V012、日/周订阅、Asia/Shanghai、调度、Mailpit/SMTP、投递审计、失败重试、一键退订 | 生产 SMTP、SPF/DKIM/DMARC 与真实收件箱验收 |
| M9 Agent | 已实现并本地验证 | V013、四类 Agent、运行/步骤/工具留痕、风险等级、L2/L3 审批、取消与结果记录 | 上线前补复杂任务质量评测和断点恢复压测 |
| M10 硬化 | 本地门禁 PASS | V014～V020、运行时 Provider/公开池/向量/最新 RAG 评测门禁、历史死信归档、Mock 关联阻断、重分析入口、保留任务、PostgreSQL/MinIO 备份及隔离恢复、全流程验收脚本 | 生产 SMTP、Linux 云服务器、域名、HTTPS、备案/合规和异机恢复演练 |

## 3. 本轮关键整改

- 现有采集记录全部保留来源审计，但 Mock 生成的中文标题、摘要、推荐理由和评分被清空；公开内容数为 0，直到真实模型重新处理并通过阈值。采集记录数会随 Worker 运行增长，因此不在进度文档固化瞬时总数。
- V016/V018 数据库触发器和 Worker 真实 Provider 门禁共同防止以后再次写入可展示的 Mock 分析或重新关联到主题事件。
- 公开接口、精选、报告、收藏和主题查询统一要求成功的非 `mock/test/fixture` Provider。
- 主题详情按发布日期分组、按发布时间倒序显示；无真实审核内容时展示诚实空状态。
- 内容评分采用《08 AI 能力设计》维度：AI 相关性、完整性、清晰度、来源证据、新颖性、影响、信息密度、营销惩罚、传闻惩罚；推荐理由必须引用文章中的具体信息，禁止“来自公开信源/通过质量分析”套话。
- RAG 配置、评测、指标、索引重建、发布就绪、邮件投递和 Agent 审批均提供管理页面。
- 管理端模型页提供“重新分析内容”批处理入口；只应在真实 Generation Provider 启用并通过烟测后执行，批次最多 500 条，重新生成非 Mock 分析并重新走发布阈值。
- 新增 `scripts/complete-m7-m10.ps1`：只有三个真实 Provider 均通过烟测后，才会激活治理配置、分批重处理历史内容、等待 Worker、回填向量、执行带引用研究、运行黄金集并要求 M10 门禁 `PASS`。
- 浏览器自动化固定使用项目根目录 `agent-browser.json` 的无头模式；测试结束必须关闭 Session，避免 Chrome for Testing 小窗闪烁。

## 4. 数据与运行状态

- Flyway：V001～V023，只前进不修改历史迁移；V021 加固时间、公开流和混合检索索引，V022 保留历史引用并允许重建 Chunk，V023 从标题/链接文本中回填可验证的英文来源日期，修正历史内容误排到最新的问题。
- 业务表：54 张；`automation` 9、`content` 13、`governance` 4、`iam` 7、`knowledge` 10、`messaging` 3、`research` 3、`source` 5。
- 信源目录当前有 122 个 Endpoint 记录，其中 79 个 `ACTIVE + HEALTHY`；其余为停用、重复目录或待适配入口，不进入采集。X 仅保留禁用的预留契约，不采集。
- 标准运行方式：Docker Compose；本地端口见《本地中间件与数据库连接指南》。
- 当前 Provider：DeepSeek OpenAI-compatible Generation、SiliconFlow `BAAI/bge-m3` Embedding、SiliconFlow `BAAI/bge-reranker-v2-m3` Rerank；密钥仅存本机 `.env`，未写入 Git、日志或文档。
- 历史死信不会删除：端点后续成功或已经归档时标记为 `IGNORED`；尚未恢复的失败继续保持 `PENDING` 并阻塞就绪检查。当前 `PENDING=0`。
- 2026-07-20 RAG v2 回填现场：知识库 1196 个有效文档、2684 个正文 Chunk、向量缺失 0、覆盖 69 个独立信源。V023 后公开内容 1196 条，其中 143 条仍缺原始来源时间、未来时间 0；RAG 指标按文档口径统计 162 个分块缺来源时间。界面/排序使用 `effective_published_at` 回退到平台发布时间并在监控中持续暴露，未来逐源补齐。数字会随 Worker 运行增长。
- 最近 24 小时真实公开内容中 IT之家 58 条、阿里云通义 48 条、GitHub Copilot 25 条，但精选接口对第三方单信源最多保留 2 条、官方/一手最多 4 条；最终实测精选首屏 50 条覆盖 19 个信源且 IT之家仅 2 条，解决高频媒体霸屏而不删除其可用报道。
- RAG 现场指标：最近 24 小时 20 次查询，10 次成功、10 次因开发期无匹配证据而诚实返回 `NO_EVIDENCE`；成功查询平均引用覆盖率 90.8%，平均 7.4 个独立信源、10 条上下文。当前 P95 约 20 秒，质量优先已达标，延迟仍是 Beta 前的重点优化项。
- 2026-07-20 本地备份验证：PostgreSQL 自定义格式转储 6,273,788 字节；MinIO TAR 归档 811,812,352 字节。`scripts/verify-backup-restore.ps1` 已在临时数据库和临时 MinIO 卷完成隔离恢复，验证 54 张业务表、1949 条内容及 MinIO 健康，不覆盖当前运行数据；异机恢复仍属于 Beta 前验收项。

## 5. 验收门禁

必须同时满足后才能称为 Beta 就绪：

1. `GENERATION_PROVIDER`、`EMBEDDING_PROVIDER`、`RERANK_PROVIDER` 都连接真实服务，且管理端 Provider 配置为 `ACTIVE`。
2. 真实模型重新处理内容，公开池中不存在空 Provider 或 Mock Provider。
3. RAG 黄金集通过引用支持度、ACL 零泄漏、召回和延迟阈值。
4. 邮件真实 SMTP、退订和失败重试通过。
5. PostgreSQL 与 MinIO 在独立目录/主机完成一次可验证恢复。
6. Linux + Nginx + HTTPS 连续运行验收通过。

## 6. 剩余任务

当前剩余任务不再是编写 M7～M10 基础功能，而是上线级放行与持续精修：配置生产 SMTP，完成 Linux/HTTPS/异机恢复、域名备案和内容合规；继续扩充 RAG 黄金集、Agent 压测、信源适配和 UI 细节。截图中出现过的密钥在正式公开运营前必须轮换，禁止写入 Git、日志或项目文档。Git 仍只在本地分支提交；未提供远程仓库前不擅自 push。
