# 新账号续开发提示词

下面内容可整段复制到新 Codex 窗口。它不包含任何密钥、密码或 Cookie。

```text
你现在接手本地项目 AI Hotspot，请直接在现有工程上继续，不要重新搭建，不要重做 M0～M10。

工作区：
D:\Dev\Autumn recruitment project\AI Hotspot

期望分支：
codex/ui-fidelity-prototype

交接文档编制前基线：
88974a9 fix: span public feeds across dates

注意：交接文档本身会在上述基线后产生新的本地提交。必须以 `git log -1 --oneline` 显示的最新本地提交为实际接手基线，不要 checkout 回 88974a9，不要丢弃用户已有修改。

开始前必须完整阅读：
1. AI Hotspot 项目方案/README.md
2. AI Hotspot 项目方案/00 项目总览/项目进度与剩余任务.md
3. AI Hotspot 项目方案/00 项目总览/已确认决策.md
4. AI Hotspot 项目方案/04 系统架构设计/系统总体架构.md
5. AI Hotspot 项目方案/06 数据库与数据治理/数据库表全景与字段中文说明.md
6. AI Hotspot 项目方案/08 AI 能力设计/搜索RAG与Agent详细设计.md
7. AI Hotspot 项目方案/10 测试运维与质量保障/本地中间件与数据库连接指南.md
8. AI Hotspot 项目方案/11 开发与交付计划/后续任务清单.md
9. AI Hotspot 项目方案/12 工程全景与账号切换交接/README.md
10. 12 目录下 01～07 全部文档。

第一步只做实时核验：
- git status --short --branch
- git log -1 --oneline
- docker compose ps
- docker compose logs --since 30m web core-api ai-api worker，搜索 ERROR/Exception/Traceback/FATAL
- 在不输出凭据的前提下，查 PostgreSQL 的 Flyway 版本、业务表数、公开内容、Document/Chunk/向量、Endpoint 健康、黄金集、死信和 RAG 延迟。

已知架构：
- Next.js 16 Web、Spring Boot 4.1 Core API、Python 3.12 AI API/Worker；
- Docker Compose 是唯一运行方式，共 10 个服务；标准入口 http://127.0.0.1:8088；
- PostgreSQL 16 + pgvector，8 个业务 Schema、56 张业务表，Flyway V031；
- 文档编制时数据：122 个信源 Endpoint（不是 HTTP API 数），79 ACTIVE+HEALTHY；2,999 内容，1,477 真实公开/已索引，3,213 Chunk，缺向量 0，Mock 公开 0；
- 50 条 RAG 黄金集历史回归 Recall@20=1.0、nDCG@10=0.974、引用支持率=1.0、ACL 泄漏=0；
- 最近 20 条成功 RAG 的 P95=11.970s，但过去 7 天 86 条成功样本 P95=19.490s，不能声称长窗口已达标；
- 文档编制时有 9 条 PENDING 死信，为 q.crawl.worker/source.crawl.requested 上游 403 类问题；需实时复核。

接下来不要先做 UI 重写，按以下顺序：
1. OPS-DLQ-01：定位并收敛当前 PENDING 采集死信，不删审计记录；
2. RAG-05：基于 retrieval_diagnostics.stageTimingsMs 定位 7 天 P95 长尾，优化后用同一样本和 Provider 回归，质量/引用/ACL 不得回退；
3. PERF-01：建立可版本化的全栈压测脚本与报告，实测 Web/Core/PostgreSQL/Redis/RabbitMQ/MinIO/Worker，严格区分指标目标与已验证结果；
4. CLEAN-01：独立提交删除已确认无引用的 mock-data.ts、content-card.tsx、workspace-page.tsx、topics.ts 和 5 个 Next 默认 SVG，删前重新 rg；
5. ARCH-02：分批拆分 globals.css、AI repository.py 和 Core 中过大的 RAG/信源类，优化 AI Docker 依赖缓存；不改变行为。

必须遵守：
- 保留用户已有修改；不用 git reset --hard，不擅自 checkout 覆盖；
- 密钥只能由容器/程序读取本机 .env，禁止输出、记录、复制或提交；
- Mock/Test/Fixture 不得进公开内容和 RAG，但不要删除测试需要的 Mock Provider；
- 数据库只新增 V032+ 迁移，不修改 V001～V031；
- 不采集 X 和微信公众号；
- 暂不引入 LangGraph、Kafka、Elasticsearch、Kubernetes；
- 常规测试不启动可见浏览器；UI 回归必须无头运行并关闭 Session；
- 每个任务完成后：测试、更新进度/待办/交接文档、形成本地 Git 提交、汇报剩余任务；
- 未经授权不 push。

标准验证：
pnpm check
services\core\mvnw.cmd -B test -f services\core\pom.xml
services\ai\.venv\Scripts\python.exe -m pytest services\ai\tests -q
docker compose --env-file .env.example config --quiet
scripts\verify-m7-m10.ps1 -RequireBetaReady

每次开始任务时，先用简短方案说明你将修什么、如何证明它正确，然后直接执行。遇到历史文档与实时数据冲突时，保留历史证据的时间语义，同时在现行文档写清实时状态。
```
