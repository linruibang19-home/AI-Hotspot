# 测试、性能与容量现状

> 本文严格区分“已通过的测试”“单次运行快照”“尚未执行的压测”。没有报告的项目不写 PASS。

## 1. 已建立的自动化门禁

| 层 | 标准命令 | 覆盖 |
|---|---|---|
| Web | `pnpm check` | lint、TypeScript、Node 测试、Next 生产构建 |
| Core | `services\core\mvnw.cmd -B test -f services\core\pom.xml` | Spring 单元/集成测试 |
| AI | `services\ai\.venv\Scripts\python.exe -m pytest services\ai\tests -q` | Connector、分析、API、Pipeline、Repository 结果 |
| Compose | `docker compose --env-file .env.example config --quiet` | 编排配置合法性，不启动服务 |
| M7～M10 | `scripts\verify-m7-m10.ps1 -RequireBetaReady` | Provider、公开池、索引/向量、黄金集、ACL、死信和就绪状态 |

阶段验收文档记录了当时的 PASS，但那些结果不是永久保证。2026-07-23 本次交接重跑结果：Web 18 项测试+生产构建 PASS，Core 35 项 PASS，AI 36 项 PASS，Compose 配置 PASS，`verify-m7-m10.ps1 -RequireBetaReady` 的 Admin、Provider、RAG 评测、Research、邮件、Agent 与 Readiness 全部 PASS。

## 2. 已有性能证据

| 范围 | 样本 | 结果 | 结论 |
|---|---:|---|---|
| RAG-02 固定批测 | 12 条真实问题 | 均值 7.683s，P95/最大 8.835s | 当时基线达标 |
| RAG 最近成功查询 | 20 条 | 均值 10.050s，P95 11.970s，最大 16.708s | 当前短窗口达标，存在单次超标 |
| RAG 近 7 天成功查询 | 86 条 | 均值 10.237s，P95 19.490s | 长尾不达 12s 目标 |
| 空闲容器资源 | 单次快照 | 总内存约 1.53 GiB，无异常应用日志 | 只说明空闲可运行 |
| 本机备份恢复 | 历史隔离演练 | PostgreSQL + MinIO 完成本机恢复 | 不等于异机或灾备验收 |

## 3. 尚未完成的正式压测

代码库中没有 k6、JMeter、wrk 或等价的可复现压测脚本和结果文件。以下均为“待验证目标”，不是已完成结论：

- 50 并发用户浏览公开资讯；
- 公开列表 API P95 < 500ms、详情 < 700ms、搜索 < 1s；
- 检索 < 2s、首个可见结果 < 5s、RAG 总延迟 P95 ≤ 12s；
- 管理查询 < 1s；
- Worker 在并发轮询、重试和索引重建期间的吞吐、队列积压和恢复速度；
- PostgreSQL 连接数、慢 SQL、索引命中、锁等待与 pgvector 召回/延迟曲线；
- Redis Session 和缓存命中率，RabbitMQ 队列积压，MinIO 并发读写，SMTP 投递速率；
- Agent 长任务、取消、重试、审批过期和断点恢复。

## 4. 建议的 PERF-01 压测方案

1. 只对 Docker Compose 栈的 Nginx `:8088` 入口发压，不绕过 Web/Core。
2. 新建 `scripts/load/` 并提交版本化的场景、阈值和数据准备说明；测试产物写入已忽略的 `artifacts/load/`。
3. 先做 1/10/25/50 并发的公开读基线，再测登录会话、搜索、RAG 和管理查询；不用公开数据写入型 Mock 增压。
4. 记录 P50/P95/P99、错误率、吞吐、容器 CPU/内存、DB 连接/慢 SQL、RabbitMQ 队列和外部 Provider 阶段延迟。
5. RAG 压测分两层：固定黄金集的质量+延迟回归；有严格 QPS/成本上限的外部 Provider 小并发。不得为了“压测”无限调用付费模型。
6. 压测后恢复安静期，检查死信、Outbox/Inbox、索引一致性和公开 Mock=0。

## 5. 性能判定的正确方式

- 不将 Docker Desktop 某一秒 CPU 图当成压测结果；
- 不把空闲健康当成高并发稳定；
- 不把“最近 20 条”与“24 小时/7 天”混成同一指标；
- 不在没有固定数据集、预热和外部 Provider 标识时对比优化前后数字。
