# AI Hotspot M5 内容质量、事件与审核治理验收记录

> 验收日期：2026-07-18  
> 结论：通过本地技术验收，等待产品负责人审核。

## 1. 交付结果

- Flyway 已由 007 升级到 008；现有 1160 条内容无损保留；
- 结构化 AI 加工、评分维度、模型运行记录、标签、实体、精确/近重复、事件和跨事件关系已落库；
- DeepSeek 等 OpenAI-compatible 生成接口已实现，地址、模型和密钥均由环境配置；默认 Mock 无密钥可运行；
- 内容治理 API、游客反馈 API、事件关系 API 和真实管理端页面已完成；
- X 仍只有不可启用的预留契约，微信公众号仍不在范围。

## 2. 关键集成证据

| 场景 | 结果 |
|---|---|
| 官方 Agent 内容 | `CONFIRMED / PASSED / PUBLISHED`，相关度 75、质量 73.25、最终分 74.21 |
| 未证实内容 | `UNCONFIRMED / REJECTED / PRIVATE`，未进入公开流 |
| 精确重复 | `is_duplicate=true`、相似度 1.00000、指向主条目、保持 PRIVATE |
| 跨事件 | 同一内容成功保留 2 个事件关系 |
| 下架与恢复 | `PUBLISHED → UNPUBLISHED → PUBLISHED`，可见性同步变化 |
| 审计 | `CONTENT_TAKEDOWN`、`CONTENT_RESTORE` 均有 actor、理由和前后数据 |
| 并发保护 | 使用旧 version 返回 HTTP 409 |
| 游客反馈 | 创建 OPEN 工单，管理员成功改为 RESOLVED |

## 3. 自动化结果

| 检查 | 结果 |
|---|---|
| Python Ruff | 通过 |
| Python Pytest（容器） | 25/25 通过 |
| Java Maven | 19/19 通过 |
| Web TypeScript | 通过 |
| Web ESLint | 通过 |
| Web 路由契约 | 8/8 通过 |
| Next.js 生产构建 | 通过，19 个页面完成生成 |
| Docker Compose 重建 | 通过，10 个服务正常运行，核心服务健康 |
| Flyway | V008 成功 |

## 4. 浏览器验收

测试流：登录 → 内容治理 → 事件聚类 → 返回内容队列 → 打开批准弹窗 → 验证理由必填 → 移动端重载。

- URL 与标题正确，页面不是空壳，无框架错误覆盖层；
- 实际渲染 100 条内容卡片和 3 个事件聚类；
- 治理弹窗正常，操作理由为必填；
- 登录后的页面控制台无 error/warning；
- 1440×1000 和 390×844 均无横向溢出。

内置浏览器插件仍因 `failed to write kernel assets: 系统找不到指定的路径 (os error 3)` 无法连接；按已获授权使用 Playwright 1.60 + 系统 Chrome 完成同等检查。该问题属于 Codex 浏览器运行时，不属于 AI Hotspot。

## 5. 已知限制

1. 默认 Mock 用于无密钥开发，只保证结构与规则可重复，不代表自然语言翻译质量；配置真实 Provider 后才评测中文生成质量；
2. 近重复当前使用标题 Jaccard，语义向量去重将在 M7 Embedding 阶段增强；
3. 自动事实识别只允许标记未证实，证伪必须人工操作；
4. 事件自动聚类为确定性首版，M6 将结合热点编辑工作流增强合并、拆分和主条目选择；
5. 本地集成验收产生 3 个事件和 1 条已解决测试工单，测试信源数据不会进入正式公开统计。

## 6. 下一阶段

M6 完整资讯产品与报告：把事件作为热点主对象，完成内容/事件详情、公开搜索、收藏、报告编辑生成与下架联动，并对现有日报、周报、月报 UI 接入真实事件治理结果。
