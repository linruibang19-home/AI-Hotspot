# 工程治理阶段验收记录

> 验收时间：2026-07-24  
> 范围：OPS-DLQ-02、CLEAN-01、ARCH-02、CONTRACT-01  
> 运行标准：Docker Compose

## 1. 验收结论

本阶段全部通过。代码清理、大文件首批拆分、AI 镜像依赖缓存、OpenAPI 全量契约与内容死信重放均已落地；没有新增数据库迁移，当前 Flyway 为 V035。最终 Docker Compose 10 个服务运行，Core、AI API、Web 和基础设施健康，Beta Readiness 为 PASS。

## 2. 交付结果

| 任务 | 结果 |
|---|---|
| OPS-DLQ-02 | 死信重放由仅支持 FetchJob 扩展为同时支持 ContentItem；事务内恢复内容候选状态、生成幂等 Outbox 事件并写管理员审计 |
| CLEAN-01 | 删除 4 个无引用 TS/TSX、5 个默认 SVG；精确删除已忽略的 736.6MiB `artifacts/web-runtime.tar` |
| ARCH-02 | Web CSS 拆为全局、报告、公开产品三层；AI 仓储拆为采集、内容、共享基础；依赖安装层不再因源码修改失效 |
| CONTRACT-01 | OpenAPI 达到 76 paths/84 operations，覆盖 19 个 Controller；CI 检查重复键、缺失/陈旧路由与实现漂移 |

## 3. 自动化与构建证据

| 门禁 | 结果 |
|---|---|
| Web ESLint / TypeScript | PASS |
| Web 测试 | 18/18 PASS |
| Web 生产构建 | PASS，25 个页面完成静态或动态构建 |
| Core 测试 | 36/36 PASS |
| AI 测试 | 41/41 PASS |
| AI Ruff | PASS，28 个 Python 文件格式一致 |
| OpenAPI 漂移检查 | PASS，84 个 Controller 操作全部覆盖 |
| OpenAPI 内部引用与 operationId | 193 个内部引用无缺失，operationId 无重复 |
| Docker 镜像 | Core、AI API、Worker、Web 构建通过；AI 连续构建依赖层为 CACHED |

## 4. 真实运行态证据

| 检查项 | 结果 |
|---|---|
| HTTP | Nginx 健康、Core 健康、AI 健康、首页、全部动态、研究页均返回 200 |
| 内容死信 | 6 条 Provider HTTP 503 死信全部 REPLAYED |
| 重处理结果 | PASSED 2、FAILED 2、REVIEW_REQUIRED 2；FAILED 为内容质量结论，不是新的 Provider 死信 |
| 可靠消息 | PENDING 死信 0，待发布 Outbox 0，普通工作队列 ready/unacked 均为 0 |
| 审计 | 当日 `DEAD_LETTER_REPLAYED` 管理员审计 6 条 |
| 数据 | 79 个 ACTIVE+HEALTHY Endpoint；1,781 条真实公开内容；1,781 条 INDEXED 文档；3,794 Chunk 缺向量 0 |
| 发布门禁 | Admin 登录通过，Beta Readiness PASS |
| 日志 | 最终部署后 Core、AI API、Worker、Web、Nginx 无 ERROR/Exception/Traceback/FATAL/panic 特征 |

RabbitMQ 物理 DLQ 继续保留历史消息（内容 6 条、采集 215 条）作为基础设施级追溯证据；业务数据库中的这些记录已按治理结果标记为 REPLAYED 或 IGNORED，不应直接清空物理 DLQ 来伪造健康。

## 5. 已知边界

1. `ARCH-02` 完成的是首批高收益拆分，不代表所有较大 Core Service 都应立即拆分；后续只在测试、变更频率或维护成本证明需要时继续。
2. OpenAPI 目前检查 Spring Controller 的方法与路径覆盖，不替代端到端响应 Schema 兼容性测试。
3. 两条重放内容被质量规则判为 FAILED、两条进入人工复核，符合内容治理状态机；不应为了提高“成功数”绕过质量门禁。
4. 生产 SMTP、Linux/HTTPS、异机恢复、密钥轮换和内容合规仍依赖目标环境，继续保留为 P1。
